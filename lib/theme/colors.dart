import 'package:flutter/material.dart';

const primaryColor = Color(0xFFF1A8A2);    // Soft pink
const secondaryColor = Color(0xFFF1C5A7);  // Peach
const accentColor = Color(0xFFA7D3F1);     // Light blue
const backgroundColor = Colors.white;       // White background
const textDark = Color(0xFF2D2D2D);        // Dark text
const textLight = Colors.white;            // White text
